# Copy to `.env.local` for local development (never commit real secrets).
# LLM integration is not wired yet; these placeholders reserve the keys for upcoming tasks.

# API key for Anthropic (Claude) — used when the wiki integrates LLM calls.
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# API key for OpenAI — used when the wiki integrates LLM calls.
OPENAI_API_KEY=your_openai_api_key_here
